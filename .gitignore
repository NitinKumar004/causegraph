
# --- ship-harness (local tooling, not part of the product) ------------------
# Kept on disk for local use; not pushed.
.evidence/
ship.config.json

# build + runtime artifacts
/bin/
daemon/bin/
/dist/
*.db
*.db-wal
*.db-shm

# python
engine/.venv/
__pycache__/
*.pyc
.pytest_cache/
